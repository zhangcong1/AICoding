1. agent-info-report.mdc
---
description: 把 AI 执行过程上报到协作平台。每轮回复前先维护本次会话的 pipeline-status JSON 并同步；steps 一次规划完整，写完立刻 sync。
alwaysApply: true
enabled: true
updatedAt: 2026-05-10T20:00:00.000Z
provider:
---

具体怎么写文件、走哪个脚本、字段叫什么，使用技能 `pipeline-status-report`。这条规则只管什么时候做、做什么。

## 每轮回复前先做这三件事

1. **回想一下本次会话里有没有 init 过**
   - 没 init 过 → 直接跑一次 init，不要去读 `.pipeline-session` 或任何已存在的 `pipeline-status-*.json` 来复用；新会话就用本次 init 新生成的那份。
   - 已经 init 过 → 沿用本次 init 输出的那份继续写。

2. **判断这一轮是新任务还是延续上一轮**
   - 延续：用户在追问、补充、修订当前还在跑的同一件事。
   - 新任务：跟上一条 pipeline 不是一回事，或上一条已经 `DONE`/`FAILED` 了又提了新需求。

3. **更新 JSON 并立刻同步**
   - 延续 → 在 `pipelines` 最后一条上主要改 `steps[*].status`、`current_step_order`、`overall_status`、`notes`、`skills`；**不要**把「完整规划」改成走一步补一步。
   - 新任务 → 在 `pipelines` 末尾追加**一条**新流水线（一个需求一条；init 后 `pipelines` 为空时首次规划也只追加一条），写 `title`，并把**该任务的全部规划大步骤一次性写入** `steps`（参考 `pipeline-status.example.json` 中 `pipelines[0].steps` 的写法）。
   - 每次 step 状态变化（PENDING → RUNNING → DONE/FAILED 等）都立刻 sync，不要攒到最后一起补。

## `pipelines[*].steps` = 当前任务的完整规划（一次性写全）

对 **`pipelines` 数组里的每一条流水线**：其 `steps` 表示**该子任务从开始到结束的完整大步骤规划**，须在**首次创建这条 PipelineEntry 时一次性全部写入**（允许在同一轮后续响应里修正尚未固化逻辑的笔误，但不应长期「走一步写一个」）：当前要做的标 `RUNNING`，后面要做的标 `PENDING`，确定不做的标 `SKIPPED`。**禁止**只写当前正在做的那一步；**禁止**在同一任务执行过程中逐条往后追加 step（任务边界客观变大时优先新开 PipelineEntry，与 skill 中的冻结规则一致）。

附带几条硬约束：

- 只写阶段/里程碑级**大步骤**，别把单次工具调用、小改动各拆一条；**单条 pipeline 最多 10 个 step**，超过就先合并再一次性写入完整列表。
- 进度和 `notes` 要跟实际一致，**禁止**虚构已完成的阶段。
- 如果某个大步骤明确要委派给 sub-agent，`step_name` 必须与该 agent Markdown 头里的 `name:` 逐字相同（含 `fullstack-` 等前缀）；规划前先去 `.codebuddy/agents/sub-agents/` 看一眼 `name:`，别凭记忆缩写或去前缀。不涉及 sub-agent 的步骤用语义化 kebab-case 即可。
- 同时把该 sub-agent frontmatter 里的 `skills:` 列表读出来，**完整**填到 `step.skills` 里（每条按 `{ slug: "<skill-name>" }`）；执行过程中又自主调用了别的 skill，再追加进去。**不要**写入 `pipeline-status-report`。
- 步骤 `WAITING_APPROVAL`（待确认）时：`current_step_order` 仍指向该步；只有下一步真正 `RUNNING` 后才进入下一步序号。


## 不要做的事

- 不要只改本地 JSON 而不执行同步脚本（哪怕只动了一个 step 状态）。
- 不要回头改已结束（`DONE`/`FAILED`）流水线的 steps、title 或 overall_status，那是历史。
- 不要改写顶层 `question.title/description`，它是首次 init 时定下来的。
- `main_agent` 只写在 `pipelines[0]`（且仅当实际使用主 Agent、尚未记过时）；`pipelines[1+]` 不要带 `main_agent`。

## 同步失败或环境不具备时

在这一轮里把原因说清楚，把同步命令原样交给用户去能访问平台的终端跑，不要默默跳过；失败就用同一条命令重试。
2. huachat-notify-task-done.mdc
---
description: 需要用户进行选择或者回复时，说明当前情况，调用 MCP 工具发送花茶通知， 让用户进行查看和回复（必须执行）
alwaysApply: true
enabled: true
updatedAt: 2026-04-08T05:42:39.447Z
provider: 
---

## 何时调用

1. 在回答过程中，需要用户进行选择或者回复时，说明当前情况，调用 MCP 工具发送花茶通知， 让用户进行查看和回复。
2. 每次任务执行完成后，总结任务执行情况，调用 MCP 工具发送花茶通知。

## 使用方式

工具名称：`send_huachat_notification`

参数：
- content（必填）：当前任务情况，纯文本字符串

## 调用示例

示例一: 需要用户进行确认或者回复时
工具：send_huachat_notification
参数：content = "您的开发任务，当前需要进行方案选择：方案一：10000元，方案二：20000元，请选择其中一个方案，请回到任务界面进行处理"

示例二: 任务执行完成后，总结任务执行情况
工具：send_huachat_notification
参数：content = "任务执行完成。成功创建20个员工，29个任务。"

## 注意事项

- 只需提供 content 参数，值为纯文本字符串
- 无需构造 JSON 对象
- @人员已在配置文件中预设，无需额外指定
3. ProjectRule.mdc
---
description: 
alwaysApply: true
enabled: false
updatedAt: 2026-03-26T07:37:33.569Z
provider: 
---

1，本项目采取双层spec驱动AICoding的开发方式，由AI执行行级编码，人类与AI协作进行Spec维护

2，全部spec存放在./spec文件夹下，除非我直接要求，不要增加新的spec以及在spec内扩展新的内容种类
3，spec分为Me2AI和AI2AI两层，分别置于./spec/Me2AI和.spec/AI2AI。Me2AI完全由我来维护，AI2AI完全由你来维护

4，Me2AI中的文档定位为设计意图传达以及技术约束。
你在意图识别和任务规划时，需要总是先读取其中相关内容
Me2AI/需求描述.md 中为需求细节描述
Me2AI/规范约束.md 中包括了对代码规范的要求，当你必须违反这里的要求时，停下来询问
Me2AI/任务规划.md 是我来记录任务规划，你不用关注

5，AI2AI中的文档定位为对当前代码状态的结构化总结。
AI2AI/实现方案.md 中保存当前需求的实现方案
AI2AI/代码规范校验报告.md 中保存当前需求的代码规范校验结果
4. README.md
# rules

This folder contains rules related content.