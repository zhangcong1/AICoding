---
description: 规范驱动开发核心规则与目录约定
alwaysApply: true
---

# 项目规则

本项目是规范驱动开发脚手架。会话开始时优先阅读 `CODEBUDDY.md`、`spec/AGENTS.md`、`spec/project.md`。

## 目录约定

- `spec/specs/` — 已确认系统能力（真理源，功能开发时不直接改）
- `spec/changes/` — 进行中需求迭代（proposal / design / tasks / delta specs）
- `spec/ME2AI/` — 人类 → AI 原始输入
- `spec/AI2AI/` — AI 草稿（默认不入库）
- `docs/` — 需沉淀的人类文档
- `src/` — 应用代码

## 必须遵守

1. 新功能先在 `spec/changes/{change-name}/` 写规格，用户确认后再写代码
2. 设计未确认禁止写 `src/app` 业务页和 `schema.ts` 业务表
3. 收到任务先检查 `.codebuddy/skills/` 是否有匹配 Skill
4. 网站类项目用 `@网站开发`，技术栈见 `spec/project.md`（pnpm + 本地 PostgreSQL + Drizzle）
5. 未 `pnpm build` 通过不得声称交付完成
6. 用中文与用户沟通

## 文档放置

| 内容 | 路径 |
|------|------|
| 人类需求 | `spec/ME2AI/requirements/` |
| AI 草稿 | `spec/AI2AI/drafts/` |
| 正式设计 | `spec/changes/{name}/design.md` |
| 任务清单 | `spec/changes/{name}/tasks.md` |
